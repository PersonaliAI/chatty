name: Secret scan

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v6

      - name: Scan source tree with Gitleaks
        uses: docker://zricethezav/gitleaks:latest
        with:
          args: git --redact --exit-code 1 --no-banner --log-opts="-1"
